# Ollama Proxy Configuration

# Ollama server URL
OLLAMA_URL=http://localhost:11434

# Proxy server host and port
HOST=localhost
PORT=3001

# Use 0.0.0.0 to listen on all network interfaces (public access)
# HOST=0.0.0.0

# Log directory (relative to project root)
LOG_PATH=./logs

# Server settings
REQUEST_TIMEOUT=300000
MAX_REQUEST_SIZE=50mb

# Optional: Enable detailed system monitoring
ENABLE_SYSTEM_MONITORING=true